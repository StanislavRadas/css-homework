<div class="form">
    {{#each actions}}
    <div class="form_body">
        <p class="form_title">{{heading}}</p>
        <a href="{{url}}" class="form_button">{{text}}</a>
    </div>
    {{/each}}
</div>